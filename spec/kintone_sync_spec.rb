require 'spec_helper'

describe KintoneSync do
  it 'has a version number' do
    expect(KintoneSync::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
